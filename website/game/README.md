# Unity WebGL Build Placeholder

Export the Unity WebGL build into this folder.

Expected entry file:

```text
website/game/index.html
```

Until that file exists, the showcase page displays a "Web build not exported yet" state.
